[package]
name = "onca_asset_system"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
onca_common = { path = "../onca_common" }
onca_fs = { path = "../onca_fs" }